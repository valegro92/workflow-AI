# ==============================================
# Workflow AI - Environment Variables
# ==============================================
# ⚠️ ATTENZIONE: Non committare mai il file .env con credenziali reali!
# ⚠️ Questo file (.env.example) deve contenere solo placeholder.

# Database (REQUIRED)
# Opzione 1: Neon (consigliato per Vercel)
# Get from: https://neon.tech > Your project > Connection string
# DATABASE_URL="postgresql://username:password@your-host.neon.tech/dbname?sslmode=require"

# Opzione 2: Supabase (alternativa)
# Get these from: Supabase Dashboard > Project Settings > API
SUPABASE_URL="https://your-project.supabase.co"
SUPABASE_ANON_KEY="your-anon-key-for-client-side"
SUPABASE_SERVICE_ROLE_KEY="your-service-role-key-for-server-side"

# Authentication (REQUIRED - Generate a strong random string)
# You can generate one with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET="your-super-secret-jwt-key-min-32-chars-change-this-in-production"
MIGRATION_SECRET="your-migration-secret-change-this"

# AI APIs (REQUIRED for AI features)
OPENROUTER_KEY=your_openrouter_key_here
GROQ_API_KEY=your_groq_key_here

# CSRF Protection (RECOMMENDED for production)
# Comma-separated list of allowed origins for CSRF protection
# ALLOWED_ORIGINS="https://yourdomain.com,https://www.yourdomain.com"

# Stripe (for payments - add later)
# STRIPE_SECRET_KEY=sk_test_xxx
# STRIPE_WEBHOOK_SECRET=whsec_xxx
